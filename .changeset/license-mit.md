---
"@textcortex/slidewise": patch
---

Set the package's npm license metadata to `MIT` to match the LICENSE file (Copyright 2026 TextCortex AI). v1.0.0 was published with `UNLICENSED` due to a stale `package.json` field; this republishes the same code under the correct SPDX license tag so hosts whose dependency review enforces SPDX-identifiable licenses will accept it.
